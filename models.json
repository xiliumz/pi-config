{
 "providers": {
   "openrouter": {
     "modelOverrides": {
       "deepseek/deepseek-v4-pro": {
         "name": "DeepSeek V4 Pro (via OpenRouter)",
         "thinkingLevelMap": {
           "xhigh": "xhigh",
         },
         "compat": {
           "openRouterRouting": {
             "only": ["deepseek"],
             "allow_fallbacks": false
           }
         }
       }
     }
   }
 }
}
